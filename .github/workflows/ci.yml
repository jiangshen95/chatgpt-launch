name: CI

on:
  push:
    branches: [main, master]
  pull_request:
  workflow_dispatch:

jobs:
  check:
    strategy:
      fail-fast: false
      matrix:
        include:
          # - os: ubuntu-latest
          - os: macos-latest
          - os: windows-latest
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@v5

      - name: Install Linux system dependencies
        if: runner.os == 'Linux'
        run: |
          sudo apt-get update
          sudo apt-get install -y \
            libwebkit2gtk-4.1-dev \
            librsvg2-dev \
            patchelf \
            libssl-dev \
            libayatana-appindicator3-dev

      - uses: dtolnay/rust-toolchain@stable

      - uses: actions/setup-node@v5
        with:
          node-version: 24

      - name: Install frontend dependencies
        run: npm install

      - name: Build frontend
        run: npm run build

      - name: Test core crate
        env:
          # macOS runners sometimes leave TMPDIR pointing at a /var/folders path
          # whose `T/` subdirectory no longer exists, which makes rustdoc's
          # doctest step fail with "failed to create temporary directory".
          # Point it at the runner's guaranteed temp directory instead.
          TMPDIR: ${{ runner.temp }}
        run: cargo test -p chatgpt-launcher-core

      - name: Check workspace compiles
        run: cargo check --workspace --all-targets

  build:
    if: github.event_name == 'workflow_dispatch' || github.event_name == 'push'
    needs: check
    strategy:
      fail-fast: false
      matrix:
        include:
          # - os: ubuntu-latest
          - os: macos-latest
          - os: windows-latest
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@v5

      - name: Install Linux system dependencies
        if: runner.os == 'Linux'
        run: |
          sudo apt-get update
          sudo apt-get install -y \
            libwebkit2gtk-4.1-dev \
            librsvg2-dev \
            patchelf \
            libssl-dev \
            libayatana-appindicator3-dev

      - uses: dtolnay/rust-toolchain@stable

      - uses: actions/setup-node@v5
        with:
          node-version: 24

      - name: Install frontend dependencies
        run: npm install

      - name: Build installers
        run: npm run tauri build

      - name: Upload artifacts
        uses: actions/upload-artifact@v6
        with:
          name: chatgpt-launcher-${{ matrix.os }}
          path: |
            target/release/bundle/**
            src-tauri/target/release/bundle/**
          if-no-files-found: warn
